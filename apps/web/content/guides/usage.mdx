---
title: Usage
description: Author pages and components.
---

# Usage

Write Markdown or MDX. Components like callouts, tabs, and cards are available in `.mdx` files. Variables and snippets resolve at build time, so pages ship as static HTML.

<Note>Static prose ships zero JavaScript. Only interactive components (tabs, code groups) hydrate as islands.</Note>

## Code groups

A code group switches between samples. It syncs page-wide with tabs that share a group.

<CodeGroup group="lang">

```python
print("hello from readsmith")
```

```javascript
console.log("hello from readsmith");
```

</CodeGroup>

## Diagrams

A fenced `mermaid` block renders as a diagram (themed to light and dark, lazy-loaded only on pages that use one). Large diagrams get pan, zoom, and a fullscreen view, drag to pan, scroll or use the buttons to zoom, and the corners button opens it fullscreen.

```mermaid
flowchart TB
  subgraph SRC["Content"]
    A1[".mdx / .md pages"]
    A2["docs.yaml / docs.json"]
    A3["snippets"]
    A4["images and assets"]
  end
  subgraph CFG["config"]
    B1["resolveConfig"]
    B2["discover pages"]
    B3["build nav tree"]
  end
  subgraph PIPE["mdx pipeline P1-P7"]
    C1["P1 Parse"]
    C2["P2 Transform"]
    C3["P3 Snippets and variables"]
    C4["P4 Projections"]
    C5["P5 Highlight"]
    C6["P6 Render"]
    C7["P7 Assembly"]
  end
  subgraph COMP["components"]
    D1["component registry"]
    D2["reading shell"]
    D3["island runtime"]
    D4["design tokens"]
  end
  subgraph OUT["SiteBuild"]
    E1["PageModel array"]
    E2["sitemap.xml"]
    E3["rss.xml"]
    E4["llms.txt and skill.md"]
    E5["search chunks"]
  end
  subgraph WEB["apps/web on Next.js"]
    F1["SSG per page"]
    F2["renderDocument"]
    F3["hydrate islands"]
  end
  R(("Reader"))

  A1 --> B1
  A2 --> B1
  B1 --> B2 --> B3
  A3 --> C3
  B1 --> C1
  C1 --> C2 --> C3 --> C4 --> C5 --> C6 --> C7
  B3 --> C7
  D1 --> C6
  C7 --> E1
  C7 --> E2
  C7 --> E3
  C7 --> E4
  C7 --> E5
  D4 --> D2
  E1 --> F1
  D2 --> F2
  F1 --> F2
  F2 --> G["static HTML"]
  A4 --> G
  D3 --> F3
  G --> R
  F3 --> R
  E5 -.-> S["search and Ask AI"]
```
