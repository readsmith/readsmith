{
  "name": "@readsmith/api-reference",
  "version": "0.0.0",
  "license": "FSL-1.1-MIT",
  "private": true,
  "type": "module",
  "description": "OpenAPI ingestion: parse, bundle, dereference, normalize to NormalizedSpec, derive, diff, and persist.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "@apidevtools/swagger-parser": "^10.1.0",
    "@readsmith/db": "workspace:*",
    "@readsmith/model": "workspace:*",
    "yaml": "^2.6.1",
    "zod": "^4.0.5"
  }
}
